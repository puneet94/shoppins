p This is the login view.
h1 Login
form(class="form-signin" name="loginForm" ng-submit = "login.submitLogin()" novalidate)
	input(type="email" name="email" class="form-control" placeholder="Email" ng-model="login.user.email" required)
	p(class="help-block" ng-show="loginForm.email.$dirty && loginForm.email.$invalid") please put valid email
	input(type="password" class="form-control" name="password" ng-model="login.user.password" placeholder="Password" required)
	button(type="submit" ng-disabled="loginForm.$invalid") Login

	button(ng-click="login.authenticate('facebook')") Sign in with Facebook


	

